#include <bits/stdc++.h>
using namespace std; 
void func(vector<int>&arr)
{
    arr[0]=699;
}
int main(){
    // vector<int>v(10);
    // iota(v.begin(),v.end(),1);
    // for(int i=0;i<v.size();i++)
    // cout<<v[i]<<" ";
    // cout<<endl;
    // // to change value we must use &arr in void func 
    // func(v);
    // for(int i=0;i<v.size();i++)
    // cout<<v[i]<<" ";
    // cout<<endl;
    vector<pair<int,int>>v={{1,2},{2,3},{69,71}};

return 0;
}